# Local dev only. On Render, these are set automatically by render.yaml's
# buildCommand (derived from the deployed backend's URL) — you don't need to
# set them in the Render dashboard.
VITE_API_URL=http://localhost:4000/api
VITE_SOCKET_URL=http://localhost:4000
